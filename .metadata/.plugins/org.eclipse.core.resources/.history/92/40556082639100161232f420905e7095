#include <iostream>
#include <list>
using namespace std;

inline list<int> adjust (int data[], int n){
	list<int> zeros;
	list<int> unZeros;

	for (int i = 0; i < n ; i ++) {

	}
	list<int> result(zeros,unZeros);
	return result;
}

int main() {
	int n;
	cin >> n;
	int data[n];
	for (int i = 0; i < n; i ++) {
		cin >> data[i];
	}
	list<int> afterA = adjust(data,n);
	for (int i = afterA.begin(); i != afterA.end(); i ++) {
		cout << *i << " ";
	}
}

