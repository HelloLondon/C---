#include "tree.h"

int BinaryTree::height() {

	return 0;
}

void BinaryTree::add(int val) {
	if(val > root) {
		if(rightTree == NULL) {
			rightTree = new BinaryTree(val);
		} else {
			add(val);
		}
	}
}

