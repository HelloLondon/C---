#include <iostream>
#include <vector>
using namespace std;

int main() {
	int n;
	cin >> n;
	vector<int> inputList;
	/**
	 *  0 means last is deal
	 *  1 means one is input
	 *  2 means there is two already;
	 */
	int curState = 0;
	for (int i = 0 ; i < n ; i ++) {
		int each;
		cin >> each;

	}
	return 0;
}
