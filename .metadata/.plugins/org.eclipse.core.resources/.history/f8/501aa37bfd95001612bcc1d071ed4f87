#include <iostream>
using namespace std;

bool palindrome (int data[], int start, int end) {
	for (;;) {
		if (start == end) {
			return true;
		}
		if(data[start] != data[end]) {
			return false;
		}
		start ++;
		end --;
	}
}

int main() {
	int num;
	cin >> num;


	return 0;
}
