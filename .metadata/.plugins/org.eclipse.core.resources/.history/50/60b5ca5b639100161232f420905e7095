#include <iostream>
#include <list>
using namespace std;

inline list<int> adjust (int data[], int n){

}

int main() {
	int n;
	cin >> n;
	int data[n];
	for (int i = 0; i < n; i ++) {
		cin >> data[i];
	}
	list<int> afterA = adjust(data,n);
	for (int i = afterA.begin(); i != afterA.end(); i ++) {
		cout << *
	}
}

