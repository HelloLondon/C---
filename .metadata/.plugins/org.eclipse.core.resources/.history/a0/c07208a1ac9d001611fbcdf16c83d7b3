#include <vector>
using namespace std;
class CircleList {
private:
	vector<int> vector;
	auto it;
public :
	CircleList(vector<int> vector):vector(vector) {
		it = this->vector.iterator;
	}
	~CircleList(){delete vector;}
	void add(int data);
	int next();
	int size();
	void remove();

};
